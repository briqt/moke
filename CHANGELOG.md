# Changelog

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [0.1.20-rc.5]

### 修复
- **修复 mosh + tmux 连接常被误判成「没附加上 tmux」**：附加确认在连接后 1 秒就问远端"这个 tmux 有几个客户端"，而 mosh 要先引导 mosh-server、再由启动命令里的 tmux 去附加，中间有几秒——数到 0 就被当成结论。后果是一个其实已经附加成功的会话被永久标成未附加：面板给出「加入」（再开一个客户端而不是返回）、detach 只说「会话已结束」、tmux 接管滚屏的绑定也不会下发。现在只有确认窗口内**持续**为 0 才判定未附上。

## [0.1.20-rc.4]

### 新增
- **首次连接新主机时先确认指纹**（默认开启）。此前首连是静默记录指纹的：这一刻若被中间人截住，假指纹入库后每次校验都只会确认那把假密钥，而用户全程看不到任何提示。现在会把目标地址、密钥算法和 SHA256 指纹摆出来，核对后再点「信任」；取消则中止连接、不记录任何东西。指纹变更依旧一律拒绝（与此开关无关）。设置 → 终端与输入 → 「首次连接自动信任新主机」可以回到静默记录的旧行为。
- **「跳到底部」**：滑进历史深处后，终端右下角出现一个按钮，一下回到最新输出。此前回到底部只能反复往回滑，或者随便敲一个键——而敲键是真的把字节发给远端。

### 修复
- **修复离开 tmux 后终端里仍写「会话结束」**：底部提示写的是「已离开 tmux，远端会话仍在运行」，屏幕上却写「会话结束」，两句自相矛盾。现在附加在 tmux 上的会话正常 detach 后，终端里写的是「已离开 tmux」；普通会话的文案不变。
- **修复 detach 恰好撞上附加确认往返时，界面把 tmux 会话说成「会话已结束」**：确认命令在会话结束之后才返回结果时，「0 个客户端」被当成「没附加上」并清掉了 tmux 关联——而 detach 本身就会让客户端数归零。现在会话已经结束时不再据此判定。
- **修复 tmux 接管滚屏时滑动过冲**：一次滑动就把整段历史穿掉，很难停在想看的位置。客户端已经按"手指走过几行"发出等量滚轮事件，而远端 tmux 默认每个事件再滚 5 行，叠加滑动惯性后就是十倍量级的过冲。现在远端每个滚轮事件只滚一行，滑动与内容 1:1 跟手；备用屏（less / man / vim）里的翻页也一并收敛成一行一次。
- **修复上传成功后看不到刚传上去的文件**：远端文件列表不会自动刷新，底部传输条还显示着上一次传输的「已完成」，看着像上传没发生。现在上传完成会刷新正在看的那个目录，传输条也改为显示最近那一条。

### 优化
- **后台保活通知的授权改到第一次开会话时才问**：此前冷启动就弹窗，那时用户还没连过任何主机，无从判断这个权限是做什么用的；而这条通知只在有会话时才存在。
- **会话列表可以一次清理已结束的会话**：存在已结束会话时列表顶部给出「清理已结束 (N)」，不必一条条关。
- **tmux 会话数角标改用中性色**：原来是告警红，看着像未读消息或出错，其实只是"远端有几个 tmux 会话可以进"。

## [0.1.20-rc.3]

### 修复
- **修复主机编辑可能被静默回滚**：会话开着时修改这台主机（名称、地址、端口、凭据、跳板机等），随后在该会话上「重新连接」或「复制会话」，改动会被还原成会话打开时的样子。同一原因还会吃掉「记住的 tmux 会话名」——于是选择器承诺的"下次连接直接进入"失效、每次连接都再问一遍。现在这类"顺手记一下"的字段（最近连接时间、tmux 会话名）按字段写入，不再整条覆盖。
- **修复关闭 mosh 会话后远端仍留着 mosh-server**：mosh 服务端按漫游语义设计，客户端消失只当作网络断了、会一直等下一个客户端。此前关闭会话只结束本地客户端，远端每次都留下一个进程和一个占用的 UDP 端口，直到主机重启；tmux 也会因此一直显示为「已附加」，面板里的客户端数不准。现在关闭会话会让客户端走 mosh 自己的退出流程通知服务端结束，并额外给服务端设了失联上限（24 小时），应用被强制停止这类情况下远端最终也会自己退出。远端 tmux 会话本身不受影响，照旧继续运行。
- **修复进入终端后外接键盘打不进字**：终端页打开时键盘焦点没交给终端本身，蓝牙/USB 键盘敲的内容全部落空，而第一次回车会触发顶栏的返回键、直接退出会话（软键盘不受影响，因为点击终端时会顺带取得焦点）。现在进入终端即取得键盘焦点，不再需要先点一下终端正文。

## [0.1.20-rc.2]

### 修复
- **修复英文界面里仍出现中文的会话状态文案**（[#1](https://github.com/briqt/moke/issues/1)，感谢 @PuggNerd 的精确复现步骤）。两个原因：
  - `[会话结束 (code N)]` 与 `[连接失败: …]` 是写死在终端内核里的中文——那一层拿不到应用资源，无论选什么语言都只会出中文。
  - 更普遍的一层：连接提示、传输错误、通知这些由后台产生的文案，取字符串时跟随的是**系统**语言而不是应用内选择的语言。于是「系统中文 + 应用内选英文」时，界面是英文，这些文案却仍是中文（反之亦然）。现在它们一律跟随应用内语言，切换后立即生效，无需重启应用。
- 一并补齐其余漏翻：字体下载 / 导入失败的提示（无下载地址、sha256 校验失败、压缩包内找不到字体、文件不可读、不是有效字体）。

### 优化
- 新增一条构建期检查：产品代码里再出现面向用户的中文字面量就会让单元测试失败，这类漏翻不再依赖人工核对。

## [0.1.20-rc.1]

### 修复
- **修复「全屏程序内滑动」的两档设置会漏到普通命令行**：选「始终发方向键」后，在 shell 提示符上滑动会去翻命令历史、把正在输入的命令换掉；选「始终发滚轮」则会把滚轮转义序列直接打进命令行。两种情况下本地滚屏历史还都彻底滚不动。这三档现在严格只在全屏程序内生效，普通命令行一律滚本地历史。
- **修复 tmux 会话重连后不再核对是否真的附加上**：点「重新连接」后，之后 detach 会被显示成「会话已结束」；万一实际没附加上（远端缺 tmux 或终端类型不可用），顶栏还会继续把它标成 tmux 会话。现在重连与首次附加一样会核对。
- **修复凭据无法解密时可能静默丢失全部连接**：连接凭据由本机密钥库里的密钥加密，而把备份恢复到新设备后那个密钥不在了。此前这会表现成「一台主机都没有」，接着任何一次写入都把原数据永久覆盖。现在这种情况会明确说明原因并拒绝写入；连接数据也不再进入系统自动备份，避免在新机上留下一份解不开的副本。加密失败时同样不再退回明文保存。
- **修复从会话列表关闭 tmux 会话时提示可能不完整**：刚从选择器新建的会话，关闭确认里不会说明远端 tmux 及其中程序会继续运行。
- **修复文件传输队列可能被一次异常卡死**：出错后队列不再停在「排队中」直到重启应用，而是把该任务标记为失败并继续后面的任务。

### 优化
- **大幅减少 mosh 主机上的 SSH 登录次数**：mosh 下管理 tmux 需要一条 SSH 控制连接，此前探测、附加确认、滚动绑定各自新建一条，进一次会话可能产生十几次登录（在服务器日志里刷屏，有登录频率限制的主机还可能因此把自己挡在门外）。现在忙时复用同一条、空闲后自动断开；连接不上时的重试也改为指数退避。
- **已经探明的 tmux 状态不再重复探测**：从文件页返回、来回切换会话不再每次都去问一遍远端。
- **SSH 连接期间显示「正在连接 …」**：此前建连与认证的这几秒里终端一片空白，分不清是在连还是卡住了。
- **字体说明在英文界面也能看到**（此前仅中文界面显示），包括大体积字体的下载提示。

## [0.1.19]

### 修复
- **修复「全屏程序内滑动」选「始终发方向键」后不生效**：远端一旦开启鼠标跟踪，该设置就被忽略。现在显式选定的模式优先于自动判定。
- **mosh 会话不再把滑动误发成方向键**：mosh 自己就运行在全屏模式里，"当前是全屏程序"这个判据在 mosh 下恒为真、不含信息量，据此发方向键在命令行上就是翻命令历史。现在这种判不出来的情况一律不发字节。

### 新增
- **让 tmux 接管滑动滚屏**（设置 → 终端与输入，默认开）：附加 tmux 会话后为该会话开启鼠标模式并绑定滚轮，由 tmux 现场判断前台程序——翻页器（less、man、vim）滑动即翻页，命令行下滑动滚 tmux 历史，自己要鼠标的程序原样转发。tmux 的历史记录在远端，不经 mosh 的屏幕差分，因此 mosh 会话也能获得与 SSH 一致的滚动体验。关掉则完全不改动远端 tmux 配置。
- **滑动确实无处可滚时给出说明**：终端顶部浮层告知原因（mosh 不传输滚动缓冲 / 全屏程序占用屏幕），并提供「改用方向键滚动」一键切换。滑动期间保持显示，消失后有静默窗口期，之后再滑仍会提示——不静默，也不刷屏。

### 优化
- 「全屏程序内滑动」三种模式的说明改写为「什么时候该选哪个」。

## [0.1.18]

### 新增
- **远端文件管理**：终端 ⋮ 与连接列表 ⋮ 新增「文件」——浏览远端目录（大小 / 时间 / 权限、隐藏文件、排序、新建文件夹、跳转到路径）、**上传**到当前目录、**下载**到本机。SSH 与 mosh 主机都支持，跳板机路由一并复用。
- **断点续传**：传输中断（取消、断网、应用被系统停止）后可继续，从断点接着传。续传前会核对远端文件是否仍是同一个（大小与修改时间），只要对不上就从头重传，绝不拼出一个看着正常的坏文件。
- **后台继续传**：退到后台或关屏时传输不中断，通知栏显示当前文件与进度。
- **把远端路径发到终端**：从终端进入文件页时，长按文件可将其完整路径（已做 shell 转义）直接送进终端输入行，省掉手打长路径。
- **下载落点**：默认存到系统「下载 / Moke」，不打断你；需要放到别处可在文件页 ⋮ 里指定目录，之后一直用它。
- **指定远端启动命令**：主机编辑页新增「启动命令」，连接后直接启动指定程序而不是默认登录 shell，可用于在 Windows 主机上选择 cmd、PowerShell 或 WSL。与「登录后自动执行」是两回事，可同时使用。
- **展开全键盘**：底部快捷键行新增「更多」键，点开一块完整键盘面板——**编辑**（Shift / Ins / Del / 翻页 / 退格 / Enter）、**功能键 F1–F12**、**控制键**（^A ^E ^U ^K ^W ^Y ^L ^D ^Z ^R ^P ^N ^B 与 Alt+Enter），一屏全见。面板浮在终端上方，不改变终端行数，全屏程序不会因展开而重绘；横屏下自动压排，三组同样一屏放得下。
- **修饰键可锁定**：Ctrl / Alt / Shift 点一下只对下一个键生效，再点一下**锁定**（连续生效，颜色区分），第三下关闭。连发 Ctrl+C 或按住 Ctrl 连走光标不必每次重按。

### 变更
- **快捷键按新标准重排**：收录标准定为「只放软键盘给不了的键」——字母、数字、标点输入法本来就能打，不再占用键位。常驻两排改为 `ESC CTRL ALT ↑ HOME END 更多` / `TAB ⇧TAB ← ↓ → ^C 文本`（Enter、退格让位给输入法给不了的 ⇧TAB 与 ^C，仍可在面板里找到）。

### 优化
- 上传时若远端已有同名文件，会先确认再覆盖。
- 下载到本机不会覆盖同名文件（自动改名保存）。
- 面板展开 / 收起为整块滑入滑出，顶部有可点的收起把手（与「更多」键互为两条出路）。

### 修复
- **修复修饰键对快捷键行无效**：此前先按 Ctrl 再按方向键、翻页、Home/End 等，发到远端的是不带修饰的序列（Ctrl 被丢掉），且 Ctrl 高亮不会熄灭、会误加到接下来输入的第一个字母上。现在这些组合按终端标准编码发送，Ctrl+← / Alt+← / Shift+Tab 均按预期生效。
- **修复会话标题有时变空或残留**：远端程序退出时重置窗口标题（发一条空标题）后，mosh 会话的标题会整条空白，SSH 会话则一直挂着上一个程序的旧标题。现在标题被清空时回落到 `用户名@主机`。
- **修复从 tmux 面板断开或关闭「连接时自动附加」的会话后，顶栏仍把该终端标成当前 tmux 会话。**
- **修复「包含预览版」开启时，发现预览版后「查看新版」仍跳转到正式版页面。**

## [0.1.17]

### 新增
- **tmux 会话管理**：终端页可直接管理远端 tmux——列出会话（真实窗口数与客户端数）、新建、重命名、打开、断开客户端、关闭。按占用状态给出「恢复 / 加入 / 返回」，另有「接管」（断开其他客户端后由本机附加）；危险操作二次确认，远端错误直接显示。**SSH 与 mosh 会话都能管理**，打开会话使用独立连接，不会向你正在运行的 Shell 或全屏程序注入命令。
- **连接即进入 tmux**：主机编辑页新增「会话持久化：无 / tmux」（默认无，现有连接行为不变）。选 tmux 后连接这台主机会先让你选会话——现有会话、新建、或打开纯 shell；选择会被记住，之后连接直接进入。
- **「全屏程序内滑动」可选**（设置 → 终端与输入）：智能（默认）/ 始终发滚轮 / 始终发方向键。
- **关于页可选「包含预览版」**（默认关）：开启后检查更新会把预览版算进来，用于抢先验证新功能。
- **关于页新增 GitHub 仓库入口**。

### 修复
- **修复全屏程序里上下滑动被当成方向键**：在 codex、claude code 这类带输入行的程序里，滑动会翻命令历史而不是看输出。智能模式下不再误发；翻页器（less、man）保持原有的滑动滚屏手感。
- **修复服务器更换主机密钥后无法再连**：删除并重新添加连接并不会清除已保存的指纹，此前只能清应用数据。主机编辑页新增「清除已保存的主机指纹」（并显示当前记录），删除连接时也会一并清除；密钥变更提示改为指向这个入口。
- **修复部分主机上打开 tmux 会话立即结束**：这些主机没有 `xterm-256color` 的终端类型定义，tmux 因此拒绝启动，而普通登录不受影响。现在附加时会自动选用该主机上真正可用的终端类型；tmux 启动失败也会保留一个可用的 shell 并把远端错误留在屏幕上，而不是只显示「会话已结束」。
- **tmux 状态不再谎报**：打开后会实际核对是否附加成功，没附上就不标成当前会话；在 tmux 里正常退出（detach）时提示「已离开 tmux，远端会话仍在运行」，与连接断开区分开；关闭 tmux 客户端时也说明远端会话及其中程序会继续运行。
- **管理目标不再混淆**：面板明确显示实际管理的配置目标与跳板路由；在终端里手动 ssh / mosh 到下一台机器后，不再假装管理内层机器，并提示把内层机器另存为连接。
- **mosh 结束不再误报连接失败**：远端程序正常退出时，PTY 关闭产生的 `EIO` 不再被二次显示为 mosh 连接错误。
- **复制会话的 `(2)` 不再永久残留**：仅在标题实际冲突时显示，标题分化后自动消失。
- **修复远端控制命令可能一直等待**（少数情况下的 SSH 控制通道超时）。

## [0.1.16]

### 新增
- **浅色主题**：新增「跟随系统 / 浅色 / 深色」，浅色下界面与系统栏一并适配；终端另配 5 套浅色配色（Solarized / GitHub / One / Catppuccin Latte / Gruvbox 亮）。
- **终端配色随主题联动**：可选开启后，浅色与深色模式各记一套终端配色，切换主题即自动换，不必再手动改。
- **键盘模式**（终端与输入设置，终端 ⋮ 内也可改）：「字符模式 / 标准 / 输入法优先」三档。部分厂商键盘在字符模式下会切到自带安全键盘、导致中文候选不可用，改为「输入法优先」即可正常输入。
- **Nerd 图标回退字体**（内置）：无论主字体是哪一款，powerline 分隔符、devicon、文件类型图标等都能正常显示，不再是方块。
- **tmux 入口常驻**：远端装了 tmux 就一直显示入口（零会话也能从面板新建），没装则不出现；有会话时才在图标上显示数量。
- **私钥默认遮蔽**：主机编辑页的私钥改为只显示类型与长度，点眼睛才展开；密码与私钥口令同样支持显隐。
- **动态取色**（Android 12+）：界面配色可取自系统壁纸。
- **关闭会话前二次确认**（默认开，可关），终端与会话列表一致。
- **终端内保持屏幕常亮**改为可关（默认开）。
- **静默检查更新**：启动时在后台检查，有新版则在「设置」与「关于」点一个主题色小圆点，不打扰。

### 优化
- **设置菜单重新分组**：一级只保留分组入口——「外观」（界面 / 字体 / 排版 / 配色 / 光标）、「终端与输入」（键盘模式 / 屏幕 / 会话提示）、「语言」、「关于」；原先平铺的主题、动态取色归入「外观」。
- 底部导航恢复标准 Material 样式（文字独立行 + 选中指示器），会话数改用角标。
- 应用图标改为终端提示符样式，更贴合工具本身。

### 修复
- **修复反复进出会话后字体退化并可能闪退**：此前每次进入终端都会重新合成一次字体，内存持续增长不释放；表现为若干次进出后字体变成系统默认、在设置里换字体也不再生效，最终因内存耗尽闪退。中文回退使用体积较大的字体（如 Maple Mono）时尤其容易触发。
- **修复终端底色不随配色**：选择非纯黑配色时，终端空白处此前显示的是应用背景色；浅色主题下更会导致文字几乎看不见。
- **修复 tmux 会话列表可能为空**：部分服务器上列表解析失败导致面板显示"没有会话"；中文会话名此前会显示成下划线，现已正常。

## [0.1.15]

- **发行变体**：新增自带 Maple Mono 的安装包（`moke-*-maple.apk`）——默认中文回退即 Maple Mono，开箱中英等宽；常规包（`moke-*.apk`）仍用内置思源黑体子集、体积更小。两者择一安装。

## [0.1.14]

- **终端**：修复 mosh 会话中在 Claude Code 等全屏应用里上下滑动被识别成方向键（误翻输入历史）的问题——现在滑动可正常滚动内容，与 SSH 表现一致。
- **关于**：修复「检查更新」点击 / 返回结果时该行文字轻微跳动（固定行高）。

## [0.1.13]

- **外观**：统一全局的标签徽标、圆角与「设置入口」样式，配色与组件更协调一致（纯视觉优化，无功能变化）。字体的能力标签在「外观」下拉与「字体管理」中显示一致。

## [0.1.12]

- **连接编辑**：选择 mosh 且配置了跳板机时给出说明——跳板机仅用于引导，mosh 会话数据走 UDP、需目标从本机可直达；目标只能经跳板机到达时建议改用 SSH。

## [0.1.9]

- **终端**：「文本段」输入改为在底部就地内联展开（替代原来的独立弹层）——打开 / 关闭更顺滑，软键盘不再收起后重新弹出。

## [0.1.8]

- **连接页**：固定按「项目」分组，去掉标题栏的分组 / 排序选择。分组可折叠 / 展开且记住折叠状态（重启保留）；长按分组头可拖动调整分组顺序；组内主机可长按拖动排序（未分组作为普通分组）。
- **会话页**：分组维度调整为「无 / 按连接 / 按项目」，排序维度调整为「创建时间 / 更新时间 / 手动」（均为组内排序）；分组之间可长按拖动排序、分组可折叠。
- **tmux 会话管理**：终端详情页新增 tmux 入口——连接后自动检测远端 tmux 会话，检测到则在顶栏显示带会话数的入口（检测不到不显示）；可查看会话列表并新建 / 重命名 / 关闭 / 附加（当前仅 SSH）。
- **终端**：进一步修复「登录后自动执行命令」偶发首字符丢失 / 回显重复——改为等终端输出稳定后再发送、并以回车符提交。

## [0.1.7]

- **会话标题**：改为双行——第 1 行动态标题、第 2 行连接名（设备名）· 协议徽标 · 状态；去掉与设备名重复的 `user@host`。移除「标题前缀」功能（分组已替代其作用）；「修改标题」仍可用（长按会话卡 / 终端 ⋮ 菜单）。
- **分组 / 排序**：连接页与会话页统一为「分组 + 排序」两个独立维度，收进标题栏右上角两个下拉——分组（连接页：无 / 项目；会话页：无 / 项目 / 主机 / 日期）、排序（名称 / 最近 / 手动）；按项目分组时分组标题已展示、卡片副标题不再重复分组名。
- **连接编辑**：「分组」改为可编辑下拉——点开选择已有分组，或直接输入新建；无主机使用的分组自动消失。「登录后自动执行」支持多行（每行一条命令）。
- **终端**：修复「登录后自动执行命令」偶发首字符丢失 / 命令错乱（改为待 shell 就绪后再发送）；选中文字菜单中文化并移除无实际作用的「更多」项；附加键回车改为文字 `Enter`、「文本段」入口改为图标；顶栏高度与其它页一致；从终端返回列表时自动收起软键盘。

## [0.1.6]

- **会话标题**：会话默认标题前缀采用创建时所用连接的名称（连接未命名则无前缀），多会话更易区分；动态标题叠加在其后。
- **会话列表**：会话卡新增「复制」——一键以同一连接再开一个独立会话，沿用原标题 / 前缀并附加不重复编号（如 `(2)`）以便区分。

## [0.1.5]

- **会话标题**：新增自定义标题与标题前缀——会话列表长按、终端 ⋮ 菜单均可设置；自定义标题优先级最高（完全覆盖），前缀叠加在动态标题之前。mosh 会话自动去除原生 `[mosh]` 前缀（协议已由徽标标识）。
- **会话列表**：卡片改为与终端顶栏同款双行布局（标题 + `user@host` · 协议徽标 · 延迟）。
- **终端**：右上角 ⋮ 菜单新增「弹出键盘 / 关闭连接」并收紧字号；粘滞修饰键 Ctrl / Alt 改为一次性（用一次即自动取消）；协议徽标进入顶栏（mosh 强调色高亮）；「发送并回车」改为文本与回车分两次发送，全屏 TUI（raw 模式）下也能正确提交而非只插入换行；底部附加键更扁、方向 / 回车符号放大。
- **界面**：各页顶栏高度收窄；底部导航去除选中态背景块、图标与文字间距收紧；外观页字号 / 间距 / 滑块更紧凑；协议徽标进一步压小、贴合文字；语言选择弹窗更紧凑；关于页收紧并移除开源许可区块；默认字号 11 → 11.5。

## [0.1.4]

- **多语言**：新增中英双语（i18n），默认跟随系统语言，设置页内可切换（跟随系统 / 中文 / English），即时生效。
- **界面**：设置各层级页标题字号统一协调；底栏图标 / 文字整体下移微调；外观实时预览示例改为更贴近真实终端的会话（提示符 + 彩色 `ls` + 代码行 + 调色板）。
- **表单**：新建 / 编辑连接的输入更紧凑精致；终端「文本段」输入框收敛留白并去掉冗余提示。
- **字体管理**：移除页内大预览（预览已在外观页），列表更聚焦。
- **文档**：README / CONTRIBUTING 提供中英双语（`*.md` 英文 · `*.zh-CN.md` 中文）。

## [0.1.3]

- **排版滑块**：行距 / 字间距范围改为绕 1.0 对称（0.7–1.3），默认值落在滑轨正中；字号上限收窄至 24（8–24），滑轨都落在实际可用区间。捏合缩放上限同步为 24。

## [0.1.2]

- **外观**：设置页按「字体 / 排版 / 配色 / 光标」分组；字号 / 行距 / 字间距改为滑块 + 步进微调；新增「恢复默认」（可撤销）；实时预览修复色带丢失并按内容收敛高度。
- **字号**：默认 11；全局 0.5 步进（设置、终端菜单、捏合缩放一致）。
- **字体管理**：已下载 / 已上传的字体排在列表前面；上传本地字体时显示进度与结果提示。
- **连接 / 会话**：支持长按拖动排序（连接页新增「手动」排序方式，顺序持久化）。
- **终端**：右上角新增折叠菜单（字号增减 / 恢复默认字号 / 显隐底部快捷键）。
- **配色**：精简为暗色方案。
- **关于**：页面信息精简。

## [0.1.1]

- **字体**：内置符号回退字体，补主/回退字体常缺的媒体 / 几何 / 杂项符号等终端字形；可下载字体扩充至十余款主流开源等宽（IBM Plex Mono、Source Code Pro、Cascadia Code、JetBrainsMono Nerd Font 等）；字体管理支持上传本地 TTF / OTF。
- **终端**：外观设置对运行中的会话即时生效；顶栏改为双行（会话名 + `用户@主机 · 协议 · 延迟`），连接信息不再单独占用终端区域；底部附加键更扁平，键位调整（右端为「文本」段入口与回车）。
- **界面**：二级页顶栏高度统一瘦身；字体预览区按内容高度收敛；文本段输入框限高、发送按钮常驻。
- **图标**：更新为水墨风格「M」应用图标。

## [0.1.0]

- **SSH**：密码 / 私钥认证；主机密钥 TOFU 校验；跳板机（ProxyJump）；登录后自动执行命令；窗口 resize；保活心跳。
- **mosh**：随包 native `mosh-client`，独立子进程 + PTY 运行，UDP 漫游。
- **多会话**：会话常驻、跨页切换；前台服务在后台保持连接。
- **连接管理**：分组 / 排序；协议徽标；复制连接命令 / 创建副本。
- **终端**：双排附加键 + 文本段输入；复制粘贴；捏合缩放；顶部状态条（协议 / 主机 / 延迟）。
- **外观**：实时预览；多套配色；字体管理（内置思源黑体子集作中文回退，可下载 Fira Code / Maple / Hack）；行距 / 字间距可调。
- **安全**：连接凭据经 Android Keystore（AES-GCM）加密存储。
